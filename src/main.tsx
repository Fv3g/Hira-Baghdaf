
import React from 'react'
import ReactDOM from 'react-dom/client'
import HiraStore from './routes/index'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HiraStore />
  </React.StrictMode>,
)
